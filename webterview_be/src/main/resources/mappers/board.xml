<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.webterview.mapper.BoardMapper">

	<!-- 게시글 resultmap -->
	<resultMap type="Board" id="articleList">
		<result column="BoardNo" property="boardNo" />
		<result column="UserNo" property="userNo" />
		<result column="BoardTitle" property="boardTitle" />
		<result column="BoardContent" property="boardContent" />
		<result column="BoardRegDate" property="boardRegDate" />
		<result column="BoardUpDate" property="boardUpDate" />
		<collection property="comments"
			column="BoardNo=boardNo" javaType="list" ofType="Comment"
			select="CommentList" />
	</resultMap>
	
	<!-- 댓글목록 조회 -->
	<select id="CommentList" resultType="Comment">
		select *
		from comment
		where BoardNo = #{boardNo}
		order by CommentNo desc
	</select>
	
	<!-- 댓글등록 -->
	<insert id="insertComment" parameterType="Comment">
		insert into comment(
		BoardNo, CommentUserNo, CommentAnswer, CommentRegDate
		) values (
		#{boardNo}, #{commentUserNo}, #{commentAnswer}, #{commentRegDate}
		)
	</insert>
	
	<!-- 댓글삭제 -->
	<delete id="deleteComment" parameterType="int">
		delete
		from comment
		where CommentNo = #{commentNo}
	</delete>
	
	<!-- 게시글 전체 조회 -->
	<select id="retrieveBoard" resultType="Board">
		select BoardNo, UserNo,
		BoardTitle, BoardContent, BoardRegDate
		from board
		order by BoardNo desc
	</select>

	<!-- 게시글 상세 조회 -->
	<select id="detailBoard" parameterType="int" resultMap="articleList">
		select BoardNo,
		BoardTitle, UserNo, BoardContent, BoardRegDate, BoardUpDate
		from board
		where BoardNo = #{boardNo}
	</select>

	<!-- 게시글 등록 -->
	<insert id="insertBoard" parameterType="Board">
		insert into board(
		BoardTitle, UserNo, BoardContent, BoardType, BoardRegDate
		) values (
		#{boardTitle}, #{userNo}, #{boardContent}, 1, #{boardRegDate}
		)
	</insert>

	<!-- 게시글 수정 -->
	<update id="updateBoard" parameterType="Board">
		update board
		set BoardTitle = #{boardTitle},
		BoardContent = #{boardContent},
		BoardUpDate = #{boardUpDate}
		where BoardNo = #{boardNo}
	</update>

	<!-- 게시글 삭제 -->
	<delete id="deleteBoard" parameterType="int">
		delete
		from board
		where BoardNo = #{boardNo}
	</delete>
	
	<!-- 게시글 총 개수 -->
	<select id="getTotalCount" resultType="int">
		select count(BoardNo)
		from board
	</select>

</mapper>

